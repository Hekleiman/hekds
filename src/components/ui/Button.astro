---
interface Props {
  variant?: 'primary' | 'secondary';
  size?: 'default' | 'small';
  href?: string;
  class?: string;
  type?: 'button' | 'submit';
}

const {
  variant = 'primary',
  size = 'default',
  href,
  class: className,
  type = 'button',
  ...rest
} = Astro.props;

const baseStyles = 'btn inline-flex items-center justify-center rounded-full font-medium text-sm whitespace-nowrap';

// Sizes scale down on small screens so buttons never feel oversized on mobile.
const sizeStyles = {
  default: 'h-10 px-4 sm:h-11 sm:px-5',
  small: 'h-8 px-3 sm:h-9 sm:px-4',
};

const variantStyles = {
  primary: 'btn-primary',
  secondary: 'btn-secondary',
};

const classes = [
  baseStyles,
  sizeStyles[size],
  variantStyles[variant],
  className,
].filter(Boolean).join(' ');
---

{href ? (
  <a href={href} class={classes} {...rest}>
    <slot />
  </a>
) : (
  <button type={type} class={classes} {...rest}>
    <slot />
  </button>
)}
